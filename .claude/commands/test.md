Run the project's test suite.

1. Run `uv run pytest -q`.
2. If any test fails, analyse the errors and propose fixes.
3. After fixing, re-run only the affected tests with `-v`.
4. Confirm that the full suite passes before continuing.
