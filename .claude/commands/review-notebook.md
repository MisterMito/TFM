Review the notebook supplied by the user: $ARGUMENTS

1. Read the notebook in full.
2. Check it follows the naming convention `N.V-initials-description.ipynb`.
3. Verify the cells were executed in sequential order.
4. Flag unused imports or dependencies not declared in `pyproject.toml`.
5. Look for hardcoded paths that should be relative or use `Path()`.
6. Suggest improvements for readability and reproducibility.
7. Verify that outputs are persisted to `data/processed/` or `reports/`.
